<!DOCTYPE html>
<html>

<head>
    <title>Department</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            margin-top: 50px;
        }

        h2 {
            color: #006699;
            font-size: 24px;
            margin-top: 0px;
            display: inline-block;
        }

        h5 {
            color: #333;
            font-size: 18px;
            display: inline-block;
        }

        .department-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 5px;
            padding: 20px;
            margin-top: 30px;
            box-shadow: 0 0 10px rgba(0, 0, 0, .2);
        }

        .department-links {
            margin: 0 0 10px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .code {
            margin: 0 0 10px 0;
        }

        .button-link {
            display: inline-block;
            background-color: #006699;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-decoration: none;
            margin-right: 5px;
        }

        .delete-container {
            display: flex;
            align-items: center;
            margin-left: 5px;
        }

        .upload-container {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
        }

        .upload-button {
            background-color: #006699;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            margin-left: 10px;
        }

        .upload-button:hover {
            background-color: #003d4d;
        }

        .upload-button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .upload-button-label {
            background-color: #006699;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            margin-right: 10px;
        }

        .upload-button-label:hover {
            background-color: #003d4d;
        }

        .upload-button-input {
            display: none;
        }

        .delete-button {
            background-color: #f44336;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            margin-left: 5px;
        }

        .delete-button:hover {
            background-color: #d32f2f;
        }

        .links-container {
            display: flex;
        }

        @media screen and (max-width: 450px) {
            .department-links {
                flex-direction: column;
            }
        }
    </style>
</head>

<body>
    <h1>Department</h1>
    {{#each departments }}
    <div class="department-container">
        <div class="department-links">
            <div class="department-name">
                <h2>{{name}}</h2>
            </div>
            <div class="links-container">
                <button class="button-link" onclick="location.href='/department/{{_id}}/edit';">Edit</button>
                <div class="delete-container">
                    <form class="delete-form" action="/department/{{_id}}?_method=DELETE" method="POST">
                        <button class="delete-button" type="submit"
                            onclick="return confirm('Are you sure you want to delete this department?')">Delete</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="code">
            <h5>Code: {{code}}</h5>
        </div>
    </div>
    {{/each}}
</body>

</html>