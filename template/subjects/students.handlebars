<!DOCTYPE html>
<html>
  <head>
    <title>Students - {{subject.name}}</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <h1>Students in {{subject.name}}</h1>
    <ul>
      {{#each user}}
      <li>{{username}}</li>
      {{/each}}
    </ul>
  </body>
</html>