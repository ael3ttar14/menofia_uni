<!DOCTYPE html>
<html>

<head>
    <title>Student Page</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #336699;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
        }

        h1 {
            margin: 0;
            font-size: 2rem;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
        }

        nav li {
            margin-right: 1rem;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            padding: 0.5rem;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        nav a:hover {
            background-color: #fff;
            color: #336699;
        }

        main {
            padding: 2rem;
        }

        h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 1rem;
        }

        th,
        td {
            padding: 0.5rem;
            border: 1px solid #ccc;
            text-align: left;
        }

        th {
            background-color: #336699;
            color: #fff;
        }

        .text-center {
            text-align: center;
        }

        .download-button {
            background-color: #006699;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            margin-left: 10px;
        }

        .download-button:hover {
            background-color: #003d4d;
        }

        .download-button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .download-button-label {
            background-color: #006699;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            margin-right: 10px;
        }

        .download-button-label:hover {
            background-color: #003d4d;
        }

        .download-button-input {
            display: none;
        }
    </style>
</head>

<body>
    <header>
        <h1>Student Page</h1>
        <nav>
            <ul>
                <li><a href="/student/register">Registration</a></li>
                <li><a href="/login">Logout</a></li>

            </ul>
        </nav>
    </header>
    <main>
        <h2>My Courses</h2>
        <table>
            <thead>
                <tr>
                    <th>Subject</th>
                    <th>Code</th>
                    <th>Name</th>
                    <th>Prerequisite for the subject</th>
                    <th>Doctor</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each subjects}}
                <tr>

                    {{!-- <td>{{this}}</td> --}}
                    <td>{{code}}</td>
                    <td>{{name}}</td>
                    <td>{{subject_depended}}</td>
                    <td>{{doctor}}</td>
                    <td class="text-center"> 
                         <form action="/student/download/{{_id}}" method="get" enctype="multipart/form-data">
                            <button id="download-button_{{code}}`" type="submit" class="download-button _{{code}}">Download</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </main>
</body>

</html>